function solve(arr){
    let arr2=[];
   for (let i = 0; i < arr.length; i++) {
        if(arr[i]<0){
           arr2.unshift(arr[i]);
        }
        else{
            arr2.push(arr[i]);
        }
    
   }
   for (let num of arr2) {
    console.log(num);
   }
}
