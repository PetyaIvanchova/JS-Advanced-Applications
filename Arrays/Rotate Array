function solve(arr, number){
   for (let index = 0; index < number; index++) {
    let n=arr.pop(arr[index]);
    arr.unshift(n);
    
   }
   let text=[];
   arr.forEach(element => {
    text.push(element);
   });
   console.log(text.join(' '));
}
